---
# General variables for all hosts

# SSH port — single source of truth
baseline_config_ssh_port: 4343
ansible_port: "{{ baseline_config_ssh_port }}"

# Admin user
baseline_config_admin_users:
  - name: hbadmin
    ssh_pubkey: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPHWyoRMlrra8cqK8ftcSLVSKTTNrcFX3qIW82oU9HRU hbadmin"

# Service user
caddy_user: hbadmin
service_nextcloud_user: hbadmin

# Caddy — Let's Encrypt email
caddy_email: ""  # PLACEHOLDER — e.g. admin@example.com

# Service domains
service_nextcloud_domain: ""  # PLACEHOLDER — e.g. cloud.example.com
service_vaultwarden_domain: ""  # PLACEHOLDER — e.g. vault.example.com
service_paperless_domain: ""  # PLACEHOLDER — e.g. docs.example.com
service_jitsi_domain: ""  # PLACEHOLDER — e.g. meet.example.com
